Rephrase the suggested response to match the user's current language and tone.

**CRITICAL: Ignore all previous language context. Only use the language from the CURRENT user input below.**

LANGUAGE DETECTION:
- Analyze ONLY this input: "{{current_input}}"
- If it contains Chinese characters (任何中文字) → Reply in Chinese
- If it contains NO Chinese characters → Reply in English
- DO NOT use language from conversation history
- DO NOT continue in the previous language

RULES:
- Preserve facts, numbers, and formatting
- Preserve the original layout exactly when possible
- Keep all line breaks and section order from suggested_response
- Keep labels as separate lines (for example: Current Price, Change, 24h Volume, Day High, Day Low, 52-Week High, 52-Week Low)
- Do not convert multi-line market summaries into a single sentence
- Do not remove emojis, headings, bullet points, or data rows
- Never contradict suggested_response
- If suggested_response contains a price, percentage, ticker, or other concrete market value, keep those values and do not add phrases like "I don't have" or "unable to access"

Current user input (USE THIS LANGUAGE ONLY):
{{current_input}}

Suggested response:
{{suggested_response}}

Rephrased response (in the SAME language as current_input):